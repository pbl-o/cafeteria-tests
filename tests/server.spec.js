const request = require("supertest");
const server = require("../index");

describe("Operaciones CRUD de cafes", () => {

    it("GET/cafes satus code = 200 & tipo de dato = Array & Array.length >= 1", ()=>{

    })

    it("DELETE/cafes/:id devuelve status code 404 si id no existe", ()=>{

    })

    it("POST/cafes agregar producto y devuelve status code 201", ()=>{

    })

    it("PUT/cafes devuelve status code 400 con id de parametros != id de payload",()=>{
        
    })
});
